# Frontend environment variables

# The backend base URL (change if you deploy elsewhere)
VITE_API_URL=https://iband-backend-first-2.onrender.com

# Optional: default admin key for local testing
# (⚠️ do NOT commit real secrets to git — only use dummy/test values here)
VITE_ADMIN_KEY=mysecret123